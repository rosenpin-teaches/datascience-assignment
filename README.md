# Data Science Assignment 2

1. Run `Rscript scripts/01_data_audit.R`.
2. Run `Rscript scripts/02_mice_imputation.R`.
3. Run `Rscript scripts/03_dummy_encode.R`.

Run the commands from the project root. The two raw Canvas CSV files are versioned in `data/raw/`. Generated processed datasets stay local; scripts and aggregate output tables are versioned.
