# Data Science Assignment 2

1. Put the two Canvas CSV files in `data/raw/`.
2. Run `Rscript scripts/01_data_audit.R` from the project root.

Raw CSV files are kept out of Git. The script saves audit tables in `outputs/01_data_audit/`.
