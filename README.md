# Data Science Assignment 2

1. Run `Rscript scripts/01_data_audit.R` from the project root.

The two raw Canvas CSV files are versioned in `data/raw/`. The script saves audit tables in `outputs/01_data_audit/`.
